import 'package:flutter/material.dart';

class RescuerScreen extends StatelessWidget {
  const RescuerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rescuer Screen')),
    );
  }
}
